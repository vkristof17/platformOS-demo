<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>ToDoList</title>
    <script src="{{ 'scripts/bootstrap.js' | asset_path }}"></script> 
    <link rel="stylesheet" href="{{ 'styles/bootstrap.css' | asset_url }}">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          {% if context.current_user %}
            <li>
              <p>Logged in as {{ context.current_user.email }}</p>
              <form action='/session/delete' method="post">
                <input type="hidden" name="authenticity_token" value="{{ context.authenticity_token }}">
                <input type="hidden" name="_method" value="delete">
                <button class="btn btn-primary" type="submit">Log Out</button>
              </form>
            </li>
          {% else %}
            <li class="nav-item">
              <a class="nav-link" href="/sign-in">Log in</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/sign-up">Sign up</a>
            </li>
          {% endif %}
          
        </ul>
      </div>
    </nav>
    
    <div class="container">
      {{ content_for_layout }}
    </div>
  </body>
</html>
